services:
  - type: web
    name: varxr-web
    runtime: static
    plan: free
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://varxr-api.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
